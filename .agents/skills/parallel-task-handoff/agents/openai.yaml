# Codex skill configuration for parallel-task-handoff.
# Manual-only: do not let the model invoke this implicitly; the user (or an agent that has
# explained why parallel work is appropriate) should invoke it explicitly.
# NOTE: confirm the exact current openai.yaml schema (and whether `allow_implicit_invocation`
# is nested under `policy:` or top-level) against official Codex docs before relying on it:
# https://developers.openai.com/codex/skills
policy:
  allow_implicit_invocation: false
