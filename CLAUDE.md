@AGENTS.md

# Claude Code Notes

Claude-native skills live in `.claude/skills/`.

At session start, read `_project-tracker/TRACKER.md`. Use `_project-tracker/NEXT_HANDOVER.md` only when the user asks for direct continuation or resume of prior work.

If an existing project's `agents.md`/`AGENTS.md` forbids a separate `CLAUDE.md`, the installer must ask before creating this file.
